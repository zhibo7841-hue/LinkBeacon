# LinkBeacon v0.7.1 Release Readiness

Audit date: 2026-09-20 (Asia/Shanghai)

## Executive Summary

LinkBeacon v0.7.1 is **Release Ready**. The Device Center Polish + Port Scan
scope is feature-complete and frozen. Automated gates, signing verification,
artifact identity, 16 KB compatibility, and the representative full RC
regression are recorded below. The exact frozen signed RC1 passed the release
gate without any runtime change or artifact rebuild.

## Formal Scope

- Unified editing of Custom Name, Device Type, and Notes.
- Read-only Local Profile summary, atomic save, and dirty-state Back handling.
- Port Scan from Tools and Device Detail.
- Audited 24-port Quick Scan and inclusive custom ranges within `1..65535`.
- Bounded, cancellable TCP Connect scanning with network-change protection.
- Real progress, Stop, partial-result retention, open-port presentation,
  aggregate non-open results, and common-service hints.
- English and Simplified-Chinese presentation.

The frozen scope excludes SSL/TLS, Website Diagnostics, Wi-Fi Analyzer, UDP or
SYN scanning, banner grabbing, service fingerprinting, Port Scan History,
Report integration, IPv6 Port Scan, and background scanning.

## Feature Complete Evidence

The dedicated device-profile editor, shared Port Scan Core, bilingual Tools and
Device Detail integration, performance calibration, and Android 12 real-device
regression were completed before RC preparation. This task changes version
metadata and release documentation only; it adds no new runtime feature.

## Version and Application Identity

- `versionName=0.7.1`
- `versionCode=8`
- `applicationId=com.networktoolbox`
- `namespace=com.networktoolbox`
- `minSdk=31`; `targetSdk=36`; `compileSdk=36`

Version code 8 was not present in repository history, tags, or published
releases before this preparation.

## Database Compatibility

- Room database version remains 5.
- The registered additive migration chain remains `MIGRATION_1_2` through
  `MIGRATION_4_5`.
- No migration or destructive fallback is added in v0.7.1.
- Existing profile IDs, identity/scope, Favorites, Custom Names, Device Types,
  Notes, Wake-on-LAN settings, observation metadata, Last Seen, and History
  remain on their existing compatible paths.

## Device Edit

Custom Name, Device Type, and Notes use one ViewModel-owned draft and one atomic
repository operation. Favorite, Wake-on-LAN, identity evidence, First Seen, and
Last Seen are outside that draft. Unsaved Back requires confirmation.

## Port Scan Core

- Final bounded concurrency: 64 workers; hard cap 64.
- Port Scan connect timeout: 1000 ms.
- Existing single Port Check default timeout: 3000 ms.
- Result categories remain OPEN, CLOSED, TIMEOUT, UNREACHABLE, and ERROR.
- TIMEOUT and UNREACHABLE may share a user-facing no-response aggregate, while
  the Core retains distinct typed counts.
- Common-service labels are hints only and never service or device-type proof.

## Port Scan UI

One Activity-scoped Hilt ViewModel and one screen serve Tools and Device Detail.
Neither entry auto-starts. Only confirmed open ports receive result rows;
non-open outcomes remain aggregate counters. Caller-aware Back and last-observed
address confirmation retain the existing navigation and identity boundaries.

## Performance

Android 12 performance validation covered Quick Scan and inclusive ranges
`1..100`, `1..1024`, `1..10000`, and `1..65535`, plus concurrency 16/32/64 and
timeouts 500/1000/1500. The selected final configuration is 64 workers and a
1000 ms connect timeout. The full refused-heavy range completed in about 77.8
seconds, retained six sorted unique open ports, and returned active sockets and
file descriptors near baseline.

## Cancellation

Stop cancels the producer and workers, closes registered active sockets, rejects
late generation results, and preserves only confirmed partial results. Back
offers Keep scanning or Stop and leave; it does not silently leave work running.

## Network Change

A material network fingerprint change terminates the session through the same
socket-cleanup path and returns NETWORK_CHANGED. Results from different network
contexts are not combined.

## Data Boundaries

Port Scan writes no History and does not enter Automatic Diagnosis or Reports.
It does not update First Seen, Last Seen, profile identity, MAC, UDN, Favorite,
Custom Name, Notes, Device Type, or Wake-on-LAN configuration.

## Internationalization

Port Scan and the unified profile editor ship with English and Simplified-
Chinese resources. User-entered targets, Custom Names, Notes, addresses, and
observed metadata remain verbatim.

## Automated Gates

All automated RC-preparation gates passed against the frozen source state:

- Targeted unit-test command covering App, Common, Database, Network, Port Scan,
  and LAN Scanner: 1,066 tests; zero failures, errors, or skips.
- Full `test`: 1,770 tests across 202 XML suites; zero failures, errors, or
  skips.
- Full `lint`: PASS with 61 warnings and zero errors. The warnings are existing
  non-blocking findings; no lint failure was suppressed for this RC.
- Debug APK assembly: PASS.
- Android instrumentation APK compilation: PASS; 45 `@Test` methods in 11
  source files compile. Execution remains part of the separately authorized
  representative-device RC regression.
- Release APK assembly using the existing local signing configuration: PASS.

## Representative Full RC Regression

**PASS** on the representative Sony XQ-AT72 / Android 12 / API 31 device using
the exact frozen signed RC1 artifact. The formal baseline was the official
signed v0.7.0 artifact. RC1 was installed in place with `adb install -r`; no
uninstall, data clear, Debug APK substitution, rebuild, or re-signing occurred.

- The installed version advanced from `0.7.0` (`versionCode=7`) to `0.7.1`
  (`versionCode=8`). `firstInstallTime` and `ceDataInode` remained unchanged.
- Existing managed-device state survived the upgrade: the same logical profile,
  Favorite, Custom Name, Device Type, Notes, Wake-on-LAN MAC and UDP port,
  Last Seen, and History remained present. First Seen was not recorded in the
  v0.7.0 fixture and correctly remained absent. The release build does not
  expose the opaque numeric profile ID, so continuity was verified by the
  unchanged profile record, data identity, and absence of duplication.
- Unified profile editing passed atomic multi-field save, immediate Device
  Detail / Device Center / Search refresh, force-stop persistence, automatic
  name restoration, Device Type set/clear precedence, Notes add/edit/clear,
  and English-UI preservation of Simplified-Chinese user text.
- Port Scan passed empty Tools entry, current-address Device Detail entry,
  last-observed-address warning and no-auto-start behavior, 24-port Quick Scan,
  custom `1..100`, large-range confirmation, Stop with partial results, Back
  confirmation, and a real Wi-Fi-to-mobile network-change stop followed by a
  successful Wi-Fi Quick Scan.
- Open and closed results agreed with TCP Port Check. Service labels remained
  hints rather than detection claims. Closed/no-response outcomes stayed
  summarized, and Port Scan did not write History or enter Automatic Diagnosis.
- Port Scan did not change Last Seen, identity evidence, Device Type, Favorite,
  Custom Name, Notes, MAC/UDN, or Wake-on-LAN configuration.
- LAN Scanner, Device Center, Quick Wake, Ping, Traceroute, DNS Lookup, TCP Port
  Check, Automatic Diagnosis, old/new History, Search/Filters, navigation,
  caller-aware Back, scroll retention, and Device Center network-switch
  behavior all passed regression checks.
- English and Simplified Chinese, verbatim user data, and light/dark Device
  Detail, Device Edit, and Port Scan states were checked without blocking
  truncation, unreadable contrast, or missing controls.
- Post-regression logcat and process inspection found no app FATAL exception,
  ANR, rejected callback execution, file-descriptor exhaustion, or socket-leak
  crash signature.
- Focused JVM gates for App navigation, Device Profile, Port Scan Core and
  ViewModel, LAN Scanner, and TCP Port Check passed: 422 tests, zero failures,
  errors, or skips.

Task 103 additionally covered Quick Scan, `1..100`, `1..1024`, `1..10000`,
`1..65535`, concurrency 16/32/64, timeouts 500/1000/1500, Stop, real network
change, bilingual presentation, and light/dark presentation on the same Android
12 device. Android 16 was not executed because no Android 16 device was
available. It is not recorded as PASS and is not a blocker under the accepted
representative-device RC strategy.

## Signed RC1 Artifact

- File: `build/release-assets/v0.7.1/LinkBeacon-v0.7.1-RC1.apk`
- Size: 51,056,985 bytes.
- SHA-256:
  `A544CB8AE29410C89B316200B8AF8047CB91474FF4CE0D014101944A14835CCF`.
- Checksum file: `LinkBeacon-v0.7.1-RC1.apk.sha256`, containing the SHA-256,
  two spaces, and the exact APK filename.
- Package metadata: `com.networktoolbox`, version `0.7.1` (`versionCode=8`),
  `minSdk=31`, `targetSdk=36`.
- `apksigner verify --verbose --print-certs`: PASS using APK Signature Scheme
  v2 with one signer.
- Signer certificate SHA-256:
  `9A7886F51465240ACEC594B25EDF7C1431425AC54FB6043C61DB0C66ED8EF4D1`.
  It exactly matches the published-version v0.7.0 signed RC artifact retained
  locally for upgrade verification.
- `zipalign -c -P 16 -v 4`: PASS.
- Eight native libraries were inspected across arm64-v8a, armeabi-v7a, x86,
  and x86_64. Every ELF `LOAD` segment uses `0x4000` alignment; none is below
  16 KB.
- The copied RC1 is byte-for-byte identical to the Gradle release output and
  was not re-signed.
- APK and checksum are ignored build artifacts and are not tracked by Git.

## Known Limitations

- TCP only and IPv4 only.
- No service fingerprinting or banner retrieval.
- No Port Scan History or Diagnostic Report integration.
- No background scan.

These are intentional Phase 1 boundaries, not RC blockers.

## RC Gate

Automated, signing, artifact, secret-scan, and 16 KB compatibility preparation
gates are **PASS**. The tracked-file scan found no APK, keystore,
`key.properties`, `local.properties`, private key, credential assignment, or
high-confidence API-secret material. Local signing files remain ignored.

Representative full RC regression is **PASS**. RC Gate is **PASS**.

## Release Gate

**PASS — RC1 RELEASE READY**. The official signed v0.7.0-to-v0.7.1 in-place
upgrade and the representative full regression passed on Sony XQ-AT72 / Android
12 / API 31 using the exact frozen signed RC1. The post-regression artifact is
still 51,056,985 bytes with SHA-256
`A544CB8AE29410C89B316200B8AF8047CB91474FF4CE0D014101944A14835CCF`.
No runtime fix, rebuild, or re-signing was required. Tag and GitHub Release
creation remain separate, explicitly authorized publication actions.
